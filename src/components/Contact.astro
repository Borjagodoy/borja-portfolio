---
---

<section id="contacto">
  <div class="section-header">
    <h2 class="section-title">Contacto</h2>
    <p class="section-subtitle">
      Si buscas a alguien que conecte I+D, arquitectura y delivery, estaré encantado de hablar.
    </p>
  </div>

  <div class="contact-inner">
    <div class="contact-main">
      <p class="contact-kicker">Disponible para</p>
      <h3 class="contact-title">
        Roles Senior / Lead, proyectos de I+D<br />y consultoría técnica especializada.
      </h3>
      <p class="contact-text">
        Podemos hablar de posiciones a tiempo completo, colaboraciones como freelance o proyectos
        concretos donde necesites alguien que aterrice tecnología compleja en producto real.
      </p>

      <div class="contact-chips">
        <span>Remote-friendly</span>
        <span>Basado en Madrid</span>
        <span>Europa / Internacional</span>
      </div>

      <div class="contact-actions">
        <!-- Cambia el correo por el tuyo -->
        <a href="mailto:tu.email@dominio.com" class="btn-primary">
          Escribirme por email
        </a>

        <!-- Cambia la URL de LinkedIn -->
        <a
          href="https://www.linkedin.com/in/tu-perfil"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-secondary"
        >
          Ver perfil en LinkedIn
        </a>

        <!-- Opcional: link a tu CV en PDF -->
        <!--
        <a
          href="/cv-borja-godoy.pdf"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-secondary ghost"
        >
          Descargar CV
        </a>
        -->
      </div>
    </div>

    <div class="contact-side">
      <div class="contact-card">
        <h4>Información rápida</h4>
        <ul>
          <li><span>Rol objetivo:</span> Senior / Lead / Staff Software Engineer</li>
          <li><span>Especialidad:</span> Frontend avanzado, arquitectura web, I+D</li>
          <li><span>Colaboraciones:</span> Freelance, remoto, híbrido</li>
        </ul>
      </div>

      <div class="contact-card subtle">
        <h4>¿Qué puedo aportar?</h4>
        <p>
          Liderazgo técnico, cercanía con equipos de investigación y foco en entregar
          cosas que se usan: desde prototipos hasta plataformas en producción.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .contact-inner {
    margin-top: 1.8rem;
    display: grid;
    gap: 1.8rem;
  }

  @media (min-width: 900px) {
    .contact-inner {
      grid-template-columns: minmax(0, 2fr) minmax(0, 1.2fr);
      align-items: start;
    }
  }

  .contact-main {
    display: grid;
    gap: 0.8rem;
  }

  .contact-kicker {
    font-size: 0.78rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #9ca3af;
  }

  .contact-title {
    font-size: 1.3rem;
    line-height: 1.3;
    color: var(--text-main);
  }

  .contact-text {
    font-size: 0.9rem;
    color: var(--text-soft);
    max-width: 34rem;
  }

  .contact-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    margin-top: 0.3rem;
  }

  .contact-chips span {
    padding: 0.22rem 0.6rem;
    border-radius: 999px;
    border: 1px solid rgba(148, 163, 184, 0.45);
    font-size: 0.78rem;
    color: #e5e7eb;
    background: rgba(15, 23, 42, 0.9);
  }

  .contact-actions {
    margin-top: 0.8rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .btn-primary,
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.35rem;
    padding: 0.45rem 0.95rem;
    border-radius: 999px;
    font-size: 0.84rem;
    text-decoration: none;
    white-space: nowrap;
  }

  .btn-primary {
    border: 1px solid rgba(56, 189, 248, 0.9);
    background: rgba(56, 189, 248, 0.16);
    color: #e0f2fe;
  }

  .btn-secondary {
    border: 1px solid rgba(148, 163, 184, 0.7);
    background: transparent;
    color: #
